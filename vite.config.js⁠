import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  base: '/ffee-work-list/', // ⚠️ 必須與 GitHub 倉庫名稱一致
  build: {
    outDir: 'dist'
  }
});


