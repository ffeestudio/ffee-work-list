import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

// 假設您的 GitHub 儲存庫名稱為 "ffee-work-list"
export default defineConfig({
  plugins: [vue()],
  base: '/ffee-work-list/', // ⚠️ 請改為您的 GitHub Repository 名稱
});
